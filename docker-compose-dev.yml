version: "3.9"
services:
    apiServer:
        env_file:
            - ./backend/.env.dev
        image: "uflock/heka-backend"
        build:
            context: ./backend
            dockerfile: Dockerfile
            target: development
        volumes:
            - ./backend:/backend
        ports:
            - "3000:3000"
        networks:
            - frontend
            - backend
networks:
    frontend:
    backend:
